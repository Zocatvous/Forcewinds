

class TurnTracker():
	def __init__():
		self.current_tick = 0

	def _next_tick(tick_bool):
		self.tick = self.current_tick+1

	def _next_turn():
		if self.tick==8
			self.tick = 1
			self.turn = self.turn +1
		else:pass

